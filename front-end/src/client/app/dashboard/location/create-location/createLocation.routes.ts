import { Route } from '@angular/router';

import { CreateLocationComponent } from './index';

export const CreateLocationRoutes: Route[] = [
	{
		path: 'create-location',
		component: CreateLocationComponent
	}
];
