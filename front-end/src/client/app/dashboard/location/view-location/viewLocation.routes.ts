import { Route } from '@angular/router';

import { ViewLocationComponent } from './index';

export const ViewLocationRoutes: Route[] = [
	{
		path: 'view-location',
		component: ViewLocationComponent
	}
];
