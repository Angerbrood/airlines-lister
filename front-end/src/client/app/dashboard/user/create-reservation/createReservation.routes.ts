import { Route } from '@angular/router';

import { CreateReservationComponent } from './index';

export const CreateReservationRoutes: Route[] = [
	{
		path: 'create-reservation',
		component: CreateReservationComponent
	}
];
