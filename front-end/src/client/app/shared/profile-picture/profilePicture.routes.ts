import { Route } from '@angular/router';

import { ProfilePictureComponent } from './index';

export const ProfilePictureRoutes: Route[] = [
	{
		path: 'profile-picture',
		component: ProfilePictureComponent
	}
];
